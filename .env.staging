# Este archivo contiene credenciales y NO debe versionarse.
# Ver .gitignore — este archivo fue removido del control de versiones.
# Copia este archivo desde .env.example y configura los valores reales en el servidor.
